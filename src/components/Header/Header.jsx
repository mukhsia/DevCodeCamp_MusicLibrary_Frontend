import './Header.css';

const Header = ({}) => {
    return (
        <header className='d-flex p-5'>
            <h2 className='flex-item'>Music Library</h2>
        </header>
    );
};

export default Header;
